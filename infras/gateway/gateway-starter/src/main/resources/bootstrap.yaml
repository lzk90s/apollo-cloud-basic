spring:
  application:
    name: gateway
  cloud:
    nacos:
      discovery:
        server-addr: registry:8848
  config:
    server-addr: registry:8848
    gateway:
      discovery:
        locator:
          enabled: true